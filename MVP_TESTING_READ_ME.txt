HOW TO TEST MVP.

Confirmed to work for straight maze:

java -jar target/mazerunner.jar -i ./examples/straight.maz.txt

Add -p flag if wanted.

When using -p, code will fail if you exit boundries of maze from start or exit.

Bug will be fixed for final deliverable.
